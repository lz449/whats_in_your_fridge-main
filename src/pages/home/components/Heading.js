import { Typography } from '@mui/material'
import React from 'react'

function Heading(props) {
    return (
        <Typography {...props}>Find your recipes..</Typography>
    )
}

export default Heading
